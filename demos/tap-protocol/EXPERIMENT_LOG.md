# Experiment log: tap-protocol
Started 2026-05-28. See demos/tap-protocol/plan.md.

- 2026-05-28: Design v0 — Drafted the 4-server inline-tap verification protocol. Topology: Gateway → Tap → Host Cluster, with Tap forking a fire-and-forget copy to a Recomputation Cluster. Both the Host and Recomp Clusters reuse `modules/inference/server/main.py` (deterministic vLLM) with `modules/inference/manifests/qwen3-1.7b.manifest.json`; two vLLMs coexist on one H100 at `gpu_memory_utilization=0.45` each. `/verify` does full re-inference and bitwise UTF-8 compare against the tapped Host output; mismatches append to `alarm.jsonl`. HMAC-SHA256 SignedEnvelope with a hardcoded shared key (constant in `servers/envelope.py`); Gateway-assigned monotonic id ties request↔response↔verification. Code lives in `demos/tap-protocol/`; the headline deliverable is `scripts/launch_vast.sh` which provisions an H100, rsyncs the worktree, starts the four servers, and prints the Gateway URL. Worktree is `worktree-tap-protocol`.
- 2026-05-28: Adversarial review + plan v1 — Ran an adversarial reviewer over plan.md. Critical findings that drove changes: the repo's `qwen3-1.7b.manifest.json` pins `TRITON_ATTN` (not FLASH_ATTN required by c3), `gpu_memory_utilization=0.9` (no CLI override in `_build_vllm_cmd`), `max_num_seqs=256 + max_model_len=8192` (KV cache too big for two coexisting instances), `cuda_launch_blocking=true` (serializes everything), and GH200 hardware_profile. Fixed by introducing a demo-local `demos/tap-protocol/qwen3-1.7b-tap.manifest.json` with FLASH_ATTN, gpu_memory_utilization=0.40, max_num_seqs=8, max_model_len=2048, cuda_launch_blocking=false, H100 hardware. Also threaded `max_tokens` through the envelope (was hardcoded in two files — refactor hazard). Launcher fixes: dynamic ghcr entrypoint resolution, valid rsync `-e "ssh -p $PORT"` syntax (was `IP:$PORT:path`), `setsid` + redirected stdin to survive ssh disconnect, `CUDA_VISIBLE_DEVICES=0` pin, `RUNNER_MODEL_PATH` set after weights prefetch so vLLM doesn't race on the HF cache, sequential cluster start so KV-cache sizing isn't racy. Added a post-Popen warm-up step (one canned inference) before each cluster flips its own /health to 200 — sidesteps vLLM's `engine not initialized` window. Owned the HMAC threat-model honestly in §10 (integrity for the localhost channel only; not auth, not anti-replay). `alarm.jsonl` opened append-only.
- 2026-05-28: First successful end-to-end run on a real H100 (vast.ai instance 38256025, offer 29019359, H100 SXM 80 GB HBM3, Czechia). The first launch_vast.sh run failed in a sequence of nix-image realities the CPU smoke couldn't have surfaced: (1) `vastai create` returned a Python dict repr `'new_contract': N` with single quotes but the launcher's regex looked for `"new_contract":` (double-quoted JSON) — fixed to accept both; (2) `rsync` is not on the nix vast-test image — replaced with `tar -czf` locally + `scp -P PORT` (uppercase, not `-p` which means preserve-mtime in scp) + `python3 -m tarfile -e` on the remote; (3) `pip`/`ensurepip` missing — but `huggingface_hub` is already bundled with vllm, so `from huggingface_hub import snapshot_download` works directly with no install step; (4) `setsid` missing — replaced with `( nohup CMD </dev/null >LOG 2>&1 & )` subshell double-fork (subshell exits immediately, child reparents to init); (5) `curl` missing on the nix image — `wait_healthy` in start_servers.sh now probes with a python3 urllib heredoc, not curl; (6) the standard bash arithmetic pitfall — `(( i++ ))` returns 1 when i=0 (false in arithmetic context) and under `set -e` aborts the script silently on the very first loop iteration; replaced with `i=$((i+1))`. The actual code bug surfaced only on GPU: with `RUNNER_MODEL_PATH` set, vLLM serves under the snapshot path (not "Qwen/Qwen3-1.7B"), so the shim's hardcoded MODEL_ID got 404'd on /v1/chat/completions — added `_resolve_model_id()` to both shims that GETs /v1/models at boot and stashes the actual served name in `STATE.model_id`. One non-code lesson: SIGKILL on a running vLLM leaks the CUDA context's GPU allocation (free dropped from 79 → 14 GiB across two restart cycles), requiring `vastai reboot instance` to reclaim — when iterating, use SIGTERM and wait for the shim's signal handler to terminate vLLM cleanly. Final result on the real H100: gateway at `http://80.188.223.202:18032` (container port 8000 maps to host port 18032 — NOT 8000 directly; pull from `vastai show instance --raw`'s `ports."8000/tcp"[0].HostPort`); two real prompts ("The capital of France is" and "Write one line of Python that prints hello world:") returned Qwen3-1.7B `<think>...` completions; both were async-verified by the Recomp Cluster's independent re-inference (host_cluster vLLM on port 8022, recomp_cluster vLLM on port 8032, both with FLASH_ATTN + enforce_eager + VLLM_BATCH_INVARIANT + CUBLAS_WORKSPACE_CONFIG=:4096:8 + seed=42 + temperature=0 + gpu_memory_utilization=0.40); each produced bitwise-identical output strings; `alarm.jsonl` never created; tap.log shows `verify verdict for id=1: {'is_verified': True}`. The c3 determinism contract holds across two coexisting vLLM processes on one H100, end-to-end through the SignedEnvelope wire protocol, demo accomplished.
- 2026-05-28: Reviewer + tester follow-ups applied — Adversarial code reviewer reported zero blockers ("ship it"); CPU integration tester reported 5/5 smoke scenarios pass (happy path, forced mismatch, signature tampering 401, id correlation, bad input). Applied the high-value should-fix items: cluster shims `os._exit(1)` on warm-up failure so `start_servers.sh` can't loop forever against a dead process (host_cluster.py, recomp_cluster.py); threaded SIGTERM handlers (the previous code called `server.shutdown()` from the signal-handler thread while main was parked in `serve_forever`, which can deadlock — now spawns a daemon thread for the shutdown call, all four servers; shutdown of the 4-server stack went from >1s to 741ms in re-smoke). Gateway `--host` default flipped from `0.0.0.0` to `127.0.0.1`; `start_servers.sh` now passes `--host 0.0.0.0` explicitly so the public bind is a property of the launcher, not a careless default. `start_servers.sh` now uses a `wait_healthy <url> <label> <max_attempts> <sleep_s>` helper with bounded retry (200×3s for clusters, 30×1s for tap/gateway) instead of `until curl … do sleep 3; done` — script exits non-zero on timeout, logs the cluster label, points at $LOG. `launch_vast.sh` `pip install` step now uses `python3 -m pip` with an `ensurepip` bootstrap fallback because the nix-based vast image may not have pip on PATH; on gateway-wait timeout, the launcher now `tail`s `/root/start.out` and the per-server logs over SSH instead of silently exiting. Dropped the dead-code conditional `if runner_path: env["RUNNER_MODEL_PATH"] = runner_path` in both cluster shims (`os.environ.copy()` already carries it). Skipped four lower-priority items by reviewer/tester consensus or explicit plan decision: 400-vs-422 distinction (client doesn't care, already 400), `/verify` 4xx-on-bad-sig (plan §3 explicitly chose to keep 200 with `is_verified=false`), ns-resolution alarm timestamps (tester said "fine for the demo"), `/tmp` EACCES on shared single-user box (cosmetic). Re-ran 14 envelope unit tests + CPU 4-server `--mock` smoke after every change: all pass; happy path returns `MOCK[hi|max=8]`, no alarm written. — Built all of plan §9 in `demos/tap-protocol/`: the c3-correct manifest `qwen3-1.7b-tap.manifest.json` (validates against `manifest.v1.schema.json` and Pydantic `Manifest`; `_build_vllm_cmd` yields `--enforce-eager --gpu-memory-utilization 0.4 --max-num-seqs 8 --max-model-len 2048 --attention-backend FLASH_ATTN`), `servers/envelope.py` (HMAC over `canonical_json_bytes(data.model_dump())` — not Pydantic's JSON — so dict insertion order can't drift the signature; 32-byte hardcoded `HMAC_KEY` with assert + docstring owning the non-secret threat model; `next_id()` is a module-level lock+counter, also `_reset_id_counter_for_tests` for unittest), `gateway.py` / `tap.py` / `host_cluster.py` / `recomp_cluster.py` (stdlib `http.server` + `ThreadingMixIn`, matching repo style). The two cluster shims share a `ClusterState` with `is_warm` gated on (a) child `/health=200` poll then (b) one canned `/v1/chat/completions` warm-up before flipping their own `/health` to 200 — so the start_servers loop's `until curl` is a single signal for ready-to-serve. Boot work runs in a daemon thread so the HTTP server binds immediately. Recomp's `alarm.jsonl` is opened with `"a"` per record (lock-guarded) and writes one `canonical_json_text(record)` line; the `[ALARM]` stderr line names id+reason. `client.py` is stdlib-only `urllib.request` and exits 1 on HTTPError. Added `tests/unit/test_tap_envelope.py` with 14 cases (round-trip, tamper of signature/payload/id/empty, dict-order stability incl. nested, JSON-wire re-serialization, monotonic id sequential + 8-thread × 200 reqs concurrency invariant, key length, manual HMAC regression) — all pass under `.venv/bin/python3 -m unittest tests.unit.test_tap_envelope`. CPU smoke (`--mock` on host + recomp, all 4 servers on ports 18000/18010/18020/18030): client gets `MOCK[Mismatch test|max=64]`, recomp `--mock-output-override "DIFFERENT"` triggers the alarm path — `alarm.jsonl` got exactly one line containing `id=1`, both sha256s, and the prefixes, and the tap's async thread logged `is_verified: False` from its `/verify` call. Surprises/deviations: (1) The plan said gateway/tap files would just `from servers.envelope import …`, but `demos/tap-protocol` has a hyphen so it isn't a Python package — I added `sys.path.insert(0, DEMO_DIR)` at module top of each server file so direct `python3 demos/tap-protocol/servers/X.py` works. The test file does the same. No deviation from the wire/protocol contract. (2) The `--proxy-port` flag name (rather than `--vllm-proxy-port`) matches start_servers.sh exactly. (3) For pydantic, used `dict[str, Any]` for `payload`; pydantic accepts dict-typed BaseModel fields without further nesting, and the wire round-trip test confirms `SignedEnvelope.model_validate(json.loads(model_dump_json))` re-verifies.
- 2026-06-11: 4-scenario workload protocol + unified web frontend (branch tap-4node-graphs, merging worktree-tap-viewer + proof-compare-graph) — Generalized the 4-node stack from single-prompt verification to WHOLE-SCENARIO verification: POST /run {workload∈inference|spec|training|coding, params} executes the matching proof-compare capture harness on the Host as a subprocess, the Tap mirrors the signed (request, WorkloadResult) pair to the Recomp, and the Recomp INDEPENDENTLY RE-RUNS the entire workload and bitwise-compares canonical capture sha256s (training ⇒ "bitwise-identical LoRA fine-tune", coding ⇒ "bitwise-identical 14-call agent run"). New servers/workloads.py (registry/param-whitelist→argv, PROGRESS-streaming runner, canonical_digest, capture_to_trace via the proof-compare tracers); gateway /run is async (202+id; sync HTTP dies in proxies on a tens-of-minutes coding run) with GET /run/<id>{,/capture,/graph} — the graph endpoint builds that run's task graph in the same {scene:graph} shape the viz loads. Spec decode got its first REAL capture harness (run_spec.py wrapping demos/spec-decode's speculative_decode via a new backward-compatible on_round hook; trace_spec_real converter), upgrading the scenario from "ported rounds" provenance. Determinism hardening for cross-process re-runs: CUBLAS workspace + torch.use_deterministic_algorithms everywhere, run_lora forces attn_implementation=eager (SDPA flash/mem-efficient BACKWARD is atomics-nondeterministic), and run_coding_agent normalizes unittest output (tempdir path → <rundir>, "in 0.003s" → "in N.NNNs", 0xADDR) BEFORE feeding it back into LLM prompts — unnormalized, host and recomp would genuinely diverge in prompt tokens, not just digest. Two bugs caught by tests: harness PROGRESS dicts carry their own "type" key which clobbered the bus event type via **prog (progress now nests under "progress"); the web page's buttons stayed disabled after a replay (updateButtons read busy=true inside the finally). Frontend rebuilt as ONE page with all 4 scenarios + Demo/Live toggle: Demo replays recorded event streams (synthesized pacing — real gaps are minutes, mock gaps ~0) then opens the embedded proof-compare viz (?scene=&src= support added with vitest); Live probes /api/health and runs real jobs via /api/run → SSE → /api/graph?id. Headless gates green: verify_web.cjs (demo×2, offline probe, zero console errors) and verify_web_live.cjs (live run end-to-end through the mock stack via dev_server.cjs's Vercel emulation). 134 in-scope python tests + 28 vitest green. Data under web/data/ is currently source=mock placeholder — the UI prints the provenance label rather than claiming real; H100 session next.
- 2026-06-11: H100 protocol session + adversarial review + ship — vast.ai H100 SXM (instance 40522962, offer 29019367, Czechia, ~50 min ≈ $2): started the stack in --no-vllm workload mode (harnesses own the GPU; two idle vLLMs would have eaten the memory the 8B coding runs needed) and ran all four scenarios through the FULL protocol. Every verdict came back bitwise-green — recomp independently re-ran each whole workload and reproduced the canonical capture sha256 exactly, zero alarm lines: inference 12 fp / 52.60 GFLOP (" Paris…"); spec 95 fp / 418.96 GFLOP with 19 REAL accept/reject rounds (32/76 drafts accepted) — spec's first real-capture provenance; training 76 fp / 21.46 TFLOP, loss 5.28→2.79, last eval " that the same input always produces the same output." — the bitwise-identical CROSS-PROCESS LoRA fine-tune held (eager-attn backward + deterministic kernels were load-bearing); coding 14 calls / 4,116 fp / 195.77 TFLOP on Qwen3-8B, tests green after 1 real fix round, all 4,116 passes reproduced bit-for-bit. Live mode was verified headlessly AGAINST THE REAL GATEWAY before teardown (browser → /api/run → real spec job #6 → Verified → graph from /api/graph). Adversarial review then found 3 MAJORs, all fixed: (M1) the protocol proves the RESULT, not freshness — with fixed params an honest capture is a deterministic constant a host could replay; owned in the UI's Verification line rather than papered over; (M2) an unreachable/crashed recomp rendered as the red Mismatch badge + alarm row — false accusation on any outage; red is now reserved for the INTEGRITY_REASONS set, infra failures show amber "Unverified (reason)"; (M3) a host-side failure emitted no terminal event and bricked live mode on "Verifying…" — tap now emits run_failed from every /run error path. Minors: source-aware captions (a mock recording can no longer ship "real H100" captions; live-run graphs get their own _meta caption from the gateway); run_spec.py was missing use_deterministic_algorithms (the log's earlier "everywhere" claim was wrong — a new static test asserts both determinism guards in every harness); run_workload's timeout was dead code behind a blocking read loop → watchdog kill; SSE freshness no longer trusts GPU-box-vs-browser clock sync (arrival-based + seq dedupe across reconnects); params bounded (max_tokens≤256, k≤8, prompt≤2000 — the gateway is public on the box); gateway JOBS evicts past 32 finished. Deploy gotcha: vercel.json's cleanUrls+trailingSlash 308'd /graph/ → /graph, breaking the vite build's relative ./assets paths (blank iframe) — caught only by running the headless gate against PRODUCTION, not the local server; both options dropped. Final state: 140 python + 26 vitest green; verify_web + verify_web_live PASS locally and verify_web PASS against https://web-two-blue-30.vercel.app (zero console errors); demo data on the site is the genuine h100 recording (manifest source=h100). Instance destroyed.
- 2026-06-11: Per-node input-size annotations — every task card now shows an "in:" line (tokens ingested + prior context attended), DERIVED from the trace's exact accounting rather than stored: attended = Σ_{j=1..t}(c0+j) ⇒ c0 = attended/t − (t+1)/2, so a prefill's c0=0 falls out of the math, a decode shows "in: 1 tok + 604 ctx", a spec verify recovers its committed context, and collapsed runs show the context growth across the run ("349 tok · ctx 864→1212" — thousands separators dropped in ranges after the first render truncated "ctx 864→1,2…" at node width and lost the end value). ctx0/inputSummary are pure functions in graph-model.js (+9 vitest, 35 total); groups carry ctxFirst/ctxLast from their first/last atomic node since a summed attended has no single c0; NODE_H 66→80 (shared constant across all three layouters). Redeployed web-two-blue-30.vercel.app (headless gate PASS, zero console errors) and republished taskgraph-dss.surge.sh from the same build, so the proof-compare standalone viz gained the annotations + the _meta.captions support in one move.
- 2026-06-11: Input-size annotations moved onto incoming edges — per user feedback the "in:" line left the card body and now rides each node's FIRST incoming edge as a React Flow edge label (the edge IS the dataflow, so the annotation sits on the arrow that carries the input; a fan-in's total appears on exactly one arrow — repeating it on all k would read as k separate inputs). Root nodes have no incoming edge, so TaskNode/GroupNode float a .task-in-root badge just above the card where that edge would land (layoutGraph flags isRoot from the in-degree map; a collapsed group can be the root, e.g. the training spine's first segment, so GroupNode needed the badge too). graph-model.js refactor: inputText (bare, for edges — the arrow already says "into") with inputSummary = "in: "+inputText kept for tooltips/badges, GroupNode's groupInput moved in as groupInputText, and edgeInputLabel(n) dispatching on kind=group; layout.js labels edges (10px mono on a --bg pill) and NODE_H reverts 80→66 since the card lost its line. +6 vitest incl. two async layoutGraph cases (chain label + isRoot flags; diamond fan-in gets exactly one labeled arrow) — 41 total green. Verified headlessly on BOTH live deployments: verify_web.cjs PASS against web-two-blue-30.vercel.app (full replay → Verified → iframe) and a 4-scene screenshot sweep against taskgraph-dss.surge.sh (11/49/75/27 edge labels, 1 root badge each, zero console errors; first surge probe was a transient network timeout, bundle hash confirmed current via curl). Gotcha: verify_web.cjs takes BASE_URL as an env var, not argv — argv silently falls back to 127.0.0.1 and ERR_CONNECTION_REFUSED looks like a deploy failure.
- 2026-06-11: Graph whitelist — free-to-pass known constants. New optional graph field `whitelist: [str,...]`: a node whose recorded input text (payload.prompt) is BYTE-FOR-BYTE equal to an entry is stamped `whitelisted: true` by build_graph (modules/proof_server/graph.py; exact match only — substring/whitespace/case near-misses tested as non-matches), and the viewer drops it from input-size accounting: the incoming edge shows an italic "whitelisted" tag instead of a size, root badges show "in: whitelisted (free)", and collapse.js excludes whitelisted members from a group's displayed token/attended sums. FLOPs are deliberately untouched — the forward pass still ran; what's free is passing a publicly-known constant IN (the user's framing: "if exactly that string is passed as input to a node, it costs nothing"). Builder API: build_graph(trace, whitelist=...) with fallback to trace["whitelist"] so tracers/converters can carry one without API churn; Graph.to_dict() emits the whitelist key only when non-empty, keeping every existing graph byte-identical (verified: inference/spec/training output unchanged after regeneration). Demonstrated on the coding scene: build_all.py whitelists the agent's fixed task statement, so the orient prefill (142 tok) now reads "in: whitelisted (free)" while its 199-decode run and everything downstream keep their sizes. inputSummary was subsumed by node-aware inputBadge(n) (handles atomic/group/whitelisted uniformly — used by tooltips and root badges). +7 unittest (TestWhitelist in test_graph.py: stamping, flops-not-zeroed, near-misses, trace-key fallback, param override, to_dict round-trip, non-string rejection) and +4 vitest (46 green). Environmental snags, not regressions: the tap-protocol stack tests need .venv/bin/python3 (system python3 lacks pydantic — ports were innocent), and this worktree's venv was missing cryptography for test_proof_server_envelopes (uv-installed; 87 unit tests green). Deployed: web-two-blue-30.vercel.app (verify_web PASS, zero console errors) + taskgraph-dss.surge.sh (badge assertion green on production).
- 2026-06-11: Whitelist follow-up — the user couldn't see it because the Vercel demo's replay graphs come from data/protocol-graphs.json (built via capture_to_trace, NOT build_all), which had no whitelist; only the standalone viz's bundled graphs.json had one. Fixed at the single chokepoint both consumers share: capture_to_trace("coding", ...) now plants trace["whitelist"] = [TASK], sourced from run_coding_agent.py's own constant via importlib (stdlib-only at module level; cached) so the whitelist can never drift from what the agent actually receives — and build_graph's trace-key fallback means the gateway /graph endpoint and record_demo_data needed ZERO changes. A custom live-run prompt simply fails the byte-for-byte match and stays paid (tested). protocol-graphs.json was rebuilt from the STORED H100 captures (web/data/runs/*.capture.json) without re-recording; assert confirmed only the coding scene changed, the other three byte-identical. +2 unittest (TestCodingWhitelist: harness-constant carriage + custom-prompt-stays-paid). Redeployed Vercel and verified the REAL user path headlessly: Demo mode → Coding agent replay → graph iframe → orient badge reads "in: whitelisted (free)" (screenshot with the badge ringed; React Flow gotcha: panning the canvas for the screenshot must drag empty canvas — dragging starts a node-drag if the pointer is on a card).
