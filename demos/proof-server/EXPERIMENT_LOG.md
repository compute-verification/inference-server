# Experiment log: proof-server

Started 2026-06-01. See plan.md.

- 2026-06-01: First draft built around its own synthetic gateways and a per-pod Ed25519 attestation envelope — landed on `worktree-proof-server` as 4 commits (~1900 lines). Worked end-to-end (./demo.sh --quick → ALL PASS) but didn't touch PR #19's tap-protocol topology at all. Audit (in-thread) found ~600 lines of v0 wrapper that didn't connect to the existing demos.
- 2026-06-02: Reset and rebuilt to match Luke's "proof server as a proxy" framing. Proof server is now an HTTP process that the existing `demos/tap-protocol/servers/tap.py` forwards a copy of every verified envelope to. v0 holds a single Ed25519 keypair on the proof server itself (not per-gateway). Ledger rows are existing `SignedEnvelope.data` payloads — no new wire schema. Dropped: `gateway_attestation.v1` + `proof_server_ledger.v1` schemas, `GatewayAttestation` class, the `SyntheticGateway` + traffic-fixture path, the file-exchange-tmpdir `developer.py`/`auditor.py`. Trimmed the SP1 program (removed `parse_id` + strict-id-order loop — now implicit in the digest check). 26-test unit suite still green; canonical-JSON encoding identical between Python and SP1 (`ledger_digest` bit-for-bit). Tap patch is ~25 lines: a second daemon thread mirroring the existing `_async_verify` fan-out. demo.sh boots the four PR-#19 servers in `--mock` mode + the proof server, sends two requests, runs `audit.py`, prints ALL PASS.
