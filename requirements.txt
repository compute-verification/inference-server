# Python dependencies for the CPU / synthetic path and the test suite.
#
# Setup (uv):
#   uv venv
#   uv pip install -r requirements.txt
#   .venv/bin/python3 -m unittest discover -s tests/unit
#
# Real (GPU) inference (`--mode vllm`) additionally needs torch + vllm, which are
# heavy and CUDA-specific — see scripts/demo.sh, which provisions a CUDA venv
# (torch cu128 + vLLM 0.17.1) on an NVIDIA box.

# Core — manifest model + JSON Schema validation
# (used by the recipes, the Pipeline, the resolver/builder, and the gates).
pydantic
jsonschema

# Tests — property-based tests + the plotting scripts the suite subprocesses.
hypothesis
matplotlib
